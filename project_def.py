# This simple file defines what configuration entries are required for the
#  system to work.

NAME='lightHead'
REQUIRED=('lightHead_server',   # connection to server: ('ip_address', port)
          'face_backend',
          'spine_backend'
          )

